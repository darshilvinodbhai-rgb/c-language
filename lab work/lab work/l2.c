#include<stdio.h>
#include<conio.h>
int main(){
	float length,area;
	
	printf("enter value of length:");
	scanf("%f",&length);
	
	area = length*length;

	printf("length value is=%f",area);
	
	return 0;
	
	
}
